#Introduction

print("Hello, my name is Posibot what is your name?")
name = input("My name is: ")
print("Nice to meet you", name+"!")

#Explanation of what the bot does

print("Would you like to know more about what I do?")
infoResponse = input("Type 'y' for yes or type 'n' for no: ")

# topic = input()
while infoResponse.lower != 'esc':
  if infoResponse.lower() == 'y':
    print("My job is to detect negative comments and filter them out to make social medias more positve friendly!")
    infoResponse = input('Please enter a comment: ')
    
  
  elif infoResponse.lower() == 'n':
    print("Okay, would you like to continue on?")
    infoResponse = input("Type 'y' for yes or type 'n' for no: ")
    break 

  #else:
  else:
    break
    #infoResponse contains text/comments
    
    #process infoResponse

  
#Asks user to insert texts/comments


#Final Thank you message
print("I really enjoyed this conversation! Thank you very much and make sure to spread positvity on the internet!")
